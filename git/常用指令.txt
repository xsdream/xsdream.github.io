cat fileName 查看某个文件
git init
git add 
git commit 
git status
git diff
git log  / git log --pretty=oneline
git reset  --hard  版本号（HEAD）  HEAD代表当前版本 HEAD^代表上个  HEAD^^类推
git reflog   历史操作记录，可以回退到之前的版本
git checkout .
git checkout -- fileName
git reset 拉回暂存区所有
git reset HEAD fileName  拉回暂存区一个文件
git rm     删除文件（checkout可以还原）